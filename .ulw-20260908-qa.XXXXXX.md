# Ultrawork Notepad — independent PupZ frozen-build manual QA
Started: 2026-09-08T17:49:00+07:00

## Plan (exhaustively detailed)
1. Read current ULW state and inspect existing C001-C003 artifacts, source contract, and runtime/package prerequisites.
2. Record the LIGHT tier and exact browser/data-shaped scenarios with binary observables and adversarial classes.
3. Run C001 in fallback Playwright Chromium against the live app; capture action log, screenshot, and storage evidence; close page/context/browser and verify no QA-owned leftover process/state.
4. Run C002 with seeded current-week/prior-week/malformed/old localStorage; capture action log, screenshot, and storage evidence; close all browser resources and verify cleanup.
5. Run C003 with 120 rows plus malformed history and irrelevant key; call shipped recordPushups via page.evaluate; capture action log, screenshot, storage dump, cleanup receipt.
6. Re-inspect any current C001-C003 artifacts, compare fresh observations, and write the non-empty manual QA report at the requested path.

## Success criteria + QA scenarios
- C001 happy path: exact Node invocation using absolute cached Playwright package opens `http://localhost:5173/`, evaluates `await import("/src/lib/exercise-history.ts").then(m => m.recordPushups(13, new Date("2026-09-08T12:00:00")))`, reloads, and observes Today `13 / 30`, plus compact stored record; PASS only with screenshot/action/storage artifacts.
- C002 weekly/streak edge: exact Playwright page.evaluate seeds current-week, prior-week, malformed, and old records, reloads `/`, and observes correct This week and Current streak cards; PASS only with screenshot/action/storage artifacts.
- C003 bounded-storage regression/adversarial: exact Playwright page.evaluate seeds 120 rows, malformed history, and unrelated key, calls recordPushups(7, fixed date), reloads, and observes bounded valid JSON with new record, stale/malformed pruning, preserved unrelated key, and usable route; PASS only with screenshot/actions/storage artifacts.
- Adversarial classes: malformed input, stale state, misleading success output, dirty worktree, hung/long commands, and cleanup/leftover browser state. Classes not triggered by a scenario are recorded not_applicable with a reason.
- Stop right away when the requested report exists, has a binary verdict and non-empty artifact references, and all browser contexts/resources are closed.

## Now
Writing the required fresh manual QA matrix and verifying final cleanup.

## Todo
- Write and verify final report; verify browser/resource cleanup.

## Findings
- Existing ULW plan: `.omo/ulw-loop/01a08052-f684-7072-8ec8-ac144b95c26a/goals.json`; C001-C003 are pending.
- Live server PID 78500 listens on `[::1]:5173`; it is pre-existing and outside QA teardown scope.
- Worktree has pre-existing tracked modifications and untracked `src/lib/exercise-history.ts`; no tracked files will be edited by this QA run.
- Cached Playwright browser files exist under `/Users/neo/Library/Caches/ms-playwright/`; exact Node package path still to be resolved.
- Absolute cached package resolved: `/Users/neo/.npm/_npx/81e833f6d16d6127/node_modules/playwright-core` version 1.63.0; executable `/Users/neo/Library/Caches/ms-playwright/chromium-1234/chrome-mac-arm64/Google Chrome for Testing.app/Contents/MacOS/Google Chrome for Testing` exists.
- Existing C001/C002 screenshots independently render the reported 13/30, 03/7, 13/120 and 15/30, 03/7, 23/120 dashboard cards. Existing C003 screenshot renders 08/30, 90/7, 09/120, but its prior action log does not prove the requested 120-row cleanup scenario.
- Existing C001-C003 reports are prior execution evidence; they are not being reused as fresh PASS evidence. Fresh Playwright execution is required and is available.

## Learnings
- Evidence must be written before recording a PASS and include a cleanup receipt.
- C001 attempt 1: Node 26 rejected the here-doc because CommonJS `require` and top-level `await` were mixed (`ERR_AMBIGUOUS_MODULE_SYNTAX`); no browser launched. The retry wraps the driver in an async IIFE.
- C002 attempt 1: app observed `04 / 7` because the fixture placed prior-week data on 2026-09-05, contiguous with 2026-09-06; this was a fixture error. The retry moves prior-week noise to 2026-09-04 and uses stable route text instead of a single-line wrapped heading.
- C002 retry 1: screenshot and `getExerciseHistorySummary` showed the expected 03/7 and 23/120, but raw `innerText` contained line breaks around slash-separated values. Final capture normalizes whitespace before the UI regex.
- C002 retry 2: normalized DOM text includes the UI separator word `of` (`03 of / 7`, `23 of / 120`), so the final assertion targets those exact rendered strings while retaining the screenshot and direct summary assertions.
- C001 fresh PASS: recordPushups(13, fixed date) returned true; compact history persisted exactly; dashboard screenshot showed Today 13/30 after reload.
- C002 fresh PASS: seed with 2026-09-08=15, 9/7=8, 9/6=4, prior-week 9/4=100, old/malformed rows; shipped summary returned today=15, currentStreak=3, currentWeek=23 and dashboard cards matched.
- C003 fresh PASS: 124 raw rows (120 dated + malformed rows) reduced to exactly 90 compact valid rows after recordPushups(7); retained range is 2026-06-11..2026-09-08, today row is 8, stale/malformed rows are absent, and `pupz-qa-unrelated=keep-me` is preserved.
- Fresh Playwright runs did not create a server; pre-existing Vite PID 78500 remained on `[::1]:5173`. Each driver closed page, context, and browser; post-run process checks found no Playwright/Chromium process.

## Cleanup receipts
- C001: `page.close(); context.close(); browser.close();` and process scan showed no QA Playwright/Chromium process; pre-existing Vite 78500 intentionally left running.
- C002: `page.close(); context.close(); browser.close();` and process scan showed no QA Playwright/Chromium process; pre-existing Vite 78500 intentionally left running.
- C003: `page.close(); context.close(); browser.close();` and process scan showed no QA Playwright/Chromium process; pre-existing Vite 78500 intentionally left running.
- Durable artifact report: `/Users/neo/Dev/pupz/.omo/ulw-loop/01a08052-f684-7072-8ec8-ac144b95c26a/evidence/qa-review-fresh.md`.
- Durable fresh evidence copied to `.omo/ulw-loop/01a08052-f684-7072-8ec8-ac144b95c26a/evidence/fresh-C001`, `fresh-C002`, and `fresh-C003`; QA-owned `/tmp/pupz-qa-fresh.klD3HM` capture directory is ready for removal after final verification.
